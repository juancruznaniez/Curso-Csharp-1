using System;

namespace Ejemplos
{
    class Program
    {
        static void Main(string[] args)
        {
            // AND Y OR ....... NOT
            // AND = Y (&&)
            // OR = O (||)
            // Valor OP Valor AND Valor OP Valor.

            int edad = 20;
            char inicial = 'J';

        }
    }
}
